Read DESIGN_SYSTEM.md. Rewrite src/components/sections/HowItWorksSection.tsx completely. The current snake/zigzag layout with separate SVG lines has alignment issues that cannot be fixed incrementally. Replace with this approach:
LAYOUT: Vertical timeline, centered. A single continuous vertical line (dashed, emerald #10b981, opacity 0.25) runs down the center of the section on desktop. On each side of this line, alternating left and right, are the step cards. Step 1 and 3 on the right of the line, step 2 and 4 on the left. Each step card has a horizontal dashed connector line from the central vertical line to the card. At the junction point on the vertical line, place the step number (just the number, no box, emerald color, font-bold text-2xl).
SINGLE SVG APPROACH: Instead of multiple separate SVG elements, use ONE absolutely-positioned SVG overlay that covers the entire steps area. In this SVG, draw: (1) the main vertical dashed line from top step to bottom step, (2) horizontal branch lines from the vertical line to each card position. Calculate positions using fixed known values (step spacing, card offset). This guarantees all lines connect perfectly because they're in the same coordinate space.
ANIMATION: When section scrolls into view (useInView, once: true), animate using stroke-dashoffset: the vertical line draws from top to bottom over 3 seconds. Each horizontal branch line draws outward when the vertical line reaches that step's Y position. Each card fades in (opacity 0→1, y: 20→0) when its horizontal branch finishes drawing. Use Framer Motion for cards, SVG animate for the lines.
MOBILE: On mobile (below lg), switch to a simple vertical layout — single vertical line on the left side, all cards on the right. No alternating. Step numbers on the vertical line.
KEEP: All existing i18n integration (dict.howItWorks), the badge, title, subtitle, and CTA. Keep all text content and dictionary keys unchanged. Only change the visual layout and animation approach.
The key insight: ONE SVG with ALL connecting lines in the same coordinate space = perfect alignment. Multiple separate SVGs in different flex containers = impossible to align reliably.